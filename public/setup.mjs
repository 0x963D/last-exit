const status=document.querySelector('#status');const r=await fetch('/api/status').then(r=>r.json());status.textContent=r.connected?'TypeSafe connected.':`Not connected. Spent so far: $${r.budget.reservedUsd.toFixed(6)}.`;document.querySelector('#connect').onsubmit=async e=>{e.preventDefault();const input=document.querySelector('#key');const response=await fetch('/api/connect',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({key:input.value})});input.value='';const data=await response.json();status.textContent=data.error||'TypeSafe connected. Ready to play.';};